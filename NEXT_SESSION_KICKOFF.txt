StudyOlle MSA 프로젝트를 이어서 진행합니다.

프로젝트 루트:
/Users/shinyanggyun/Library/Mobile Documents/com~apple~CloudDocs/Spring/study/studyolle-msa/

현재 상태 요약 (2026-04-02 기준):
- Phase 1~4 전체 완성
- Phase 5 진행 중 — notification-service 소스코드 작성 완료, 기동 테스트 필요

[Phase 5 완료된 것]
- Docker 기동 완료 ✅
    zookeeper :2181, kafka :9092, rabbitmq :5672/:15672
    notification-db(postgres) :5436, redis :6379
- study-service Kafka Producer 완료 ✅
    StudyEventDto, StudyKafkaProducer 생성
    StudyService: STUDY_CREATED 발행
    StudySettingsService: STUDY_PUBLISHED / RECRUITING_STARTED / RECRUITING_STOPPED 발행
- event-service RabbitMQ Producer 완료 ✅
    EnrollmentEventDto, RabbitMQConfig, EnrollmentRabbitProducer 생성
    EnrollmentService: enroll/accept/reject/checkIn 시 발행
- notification-service 소스코드 작성 완료 ✅ (18개 파일)
    PostgreSQL :5436 (알림 영구 저장)
    Redis :6379 (읽지 않은 알림 카운터 + 중복 방지)
    Kafka Consumer: @KafkaListener(topics = "study-events")
    RabbitMQ Consumer: @RabbitListener(queues = "enrollment.queue")
    외부 API: GET/PATCH /api/notifications/** (CommonApiResponse 적용)
    내부 API: GET /internal/notifications/count/{accountId} (Redis 즉시 반환)

[다음 작업 — 순서대로 진행]

Step 1. notification-service 기동 테스트 (양균님)
  - IntelliJ에서 notification-service 기동
  - 확인 순서:
    ① localhost:8761 에서 NOTIFICATION-SERVICE 등록 확인
    ② 스터디 공개 → 로그에서 [Kafka] 수신: ... 확인
    ③ 모임 신청 수락 → 로그에서 [RabbitMQ] 수신: ... 확인
    ④ notification_db notifications 테이블에 데이터 저장 확인
    ⑤ redis-cli: GET notification:unread:{accountId} 값 확인

Step 2. api-gateway 라우팅 추가 (Claude 담당)
  - /api/notifications/** → lb://NOTIFICATION-SERVICE
  - 기동 테스트 완료 후 Claude가 application.yml 직접 수정

Step 3. frontend-service 알림 페이지 연동
  양균님 담당 (.java 소스코드 제공):
    notification/client/NotificationInternalClient.java
      → GET lb://NOTIFICATION-SERVICE/internal/notifications/count/{accountId}
    notification/controller/NotificationPageController.java
      → GET /notifications
    HomeController.java 수정
      → unreadCount 모델 추가 (nav 🔔 뱃지용)
  Claude 담당 (직접 파일 생성):
    templates/notifications.html (알림 목록 페이지)
    fragments.html 수정 (nav 🔔 뱃지 카운터 연동)

[설계 핵심 요약]
- study-service  → Kafka "study-events"       → notification-service
- event-service  → RabbitMQ "enrollment.queue" → notification-service
- notification-service → PostgreSQL (알림 저장) + Redis (카운터/중복방지)
- /api/notifications/** : 브라우저 → api-gateway → NotificationController
- /internal/notifications/count/** : frontend-service → NotificationInternalController (Redis 즉시)

역할 분담 원칙:
- Java 소스코드 (.java): 양균님 직접 작성, Claude는 소스코드 제공만
- frontend-service HTML 템플릿 (.html): Claude 직접 파일 생성
- application.yml, 문서 파일 (*.md, *.txt): Claude 직접 생성/수정 가능

현황 파악을 위해 MSA_GUIDE.md, MSA_TODO.txt 를 먼저 읽어주세요.
서비스 경로:
/Users/shinyanggyun/Library/Mobile Documents/com~apple~CloudDocs/Spring/study/studyolle-msa/services/

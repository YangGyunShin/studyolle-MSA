server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:8080"
              - "http://localhost:8090"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - PATCH
              - OPTIONS
            allowedHeaders:
              - "*"
            allowCredentials: true
      routes:
        # 내부 API 외부 접근 전면 차단 (가장 먼저 선언)
        - id: block-internal
          uri: no://op
          predicates:
            - Path=/internal/**
          filters:
            - SetStatus=403

        # 공개 API - account-service (JWT 검증 없이 통과)
        - id: account-service-public
          uri: lb://ACCOUNT-SERVICE
          predicates:
            - Path=/api/auth/**

        # 인증 필요 - account-service 보호 API
        - id: account-service-private
          uri: lb://ACCOUNT-SERVICE
          predicates:
            - Path=/api/accounts/**
          filters:
            - JwtAuthenticationFilter

        # admin-service (Phase 6)
        # JwtAuthenticationFilter → AdminRoleFilter 순서가 중요하다.
        # AdminRoleFilter 는 X-Account-Role 헤더를 읽는데, 그 헤더는 JwtAuthenticationFilter 가
        # JWT 를 검증하고 나서야 추가되기 때문이다. 순서를 바꾸면 AdminRoleFilter 가
        # 항상 헤더를 찾지 못해 모든 요청이 403 으로 차단된다.
        - id: admin-service
          uri: lb://ADMIN-SERVICE
          predicates:
            - Path=/api/admin/**
          filters:
            - JwtAuthenticationFilter
            - AdminRoleFilter

        # notification-service (Phase 5)
        - id: notification-service
          uri: lb://NOTIFICATION-SERVICE
          predicates:
            - Path=/api/notifications/**
          filters:
            - JwtAuthenticationFilter

        # event-service (study-service보다 먼저 선언 - 더 구체적인 경로)
        - id: event-service
          uri: lb://EVENT-SERVICE
          predicates:
            - Path=/api/studies/*/events/**
          filters:
            - JwtAuthenticationFilter

        # study-service
        - id: study-service
          uri: lb://STUDY-SERVICE
          predicates:
            - Path=/api/studies/**
          filters:
            - JwtAuthenticationFilter

        # frontend-service - 모든 페이지와 정적 리소스를 포괄하는 단일 라우트
        # Spring Cloud Gateway는 선언 순서대로 매칭하므로
        # 반드시 모든 /api/** 라우트 및 /internal/** 차단 이후에 선언해야 한다.
        - id: frontend-service
          uri: lb://FRONTEND-SERVICE
          predicates:
            - Path=/**
          filters:
            - OptionalJwtFilter

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

jwt:
  secret: studyolle-msa-secret-key-must-be-at-least-256-bits-long
